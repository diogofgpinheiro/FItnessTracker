<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diogo.fitnesstracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Login"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.Registar3"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.Registar"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.Registar2"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".activities.teste" />
        <activity android:name=".activities.testeBottomNav"></activity>
    </application>

</manifest>